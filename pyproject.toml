[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "BasketScoreFramework"
version = "0.1.0"
description = "Professional Basketball Scoreboard Framework"
requires-python = ">=3.13"
authors = [
	{ name = "Elver Llatas" }
]

